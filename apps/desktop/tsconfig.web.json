{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The renderer imports the preload contract for its types, so the root
    // spans both. It never imports preload *code* — only the type surface.
    "rootDir": "src",
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["node", "vite/client"],
    "skipLibCheck": true,
    "paths": {
      "@/*": ["./src/renderer/src/*"]
    },
    "baseUrl": "."
  },
  "include": ["src/renderer/**/*.ts", "src/renderer/**/*.tsx", "src/preload/contracts.ts"]
}
