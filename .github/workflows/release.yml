name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions: {}

jobs:
  release:
    permissions:
      actions: write
      attestations: write
      contents: write
      id-token: write
      packages: write
      security-events: write
    uses: ./.github/workflows/reusable-release-build.yml
